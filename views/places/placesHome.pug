extends ../layout


block content
  h1 "Places in your city" #{userCity}
  
  ul
  each place in places
    li 
        b #{place.name} 
        #{place.street} #{place.street} #{place.localNr}
    
  
  
